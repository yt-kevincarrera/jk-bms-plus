<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Android 12+ Bluetooth permissions. neverForLocation is deliberately NOT
         set on BLUETOOTH_SCAN: the app does use location, for the GPS side of
         the Wh/km calculation. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Android 11 and below. -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Android requires location permission for BLE scanning, and the app needs
         it anyway for trip distance and altitude. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- The auto-start detector needs a fix before a trip exists, and asks for
         it with the screen off. A location-typed foreground service covers that
         on most devices; this is the permission Android falls back to asking
         for when it does not, and it is requested only if the device test in
         the plan shows the service type is not enough. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- A trip keeps recording with the screen off, which on Android means a
         foreground service. Without it the OS throttles a backgrounded app to a
         few location fixes an hour and the ride becomes a straight line. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- The app talks to exactly one host on the internet: api.github.com, to
         ask whether a newer release has been published, and to fetch it when
         the rider says so. Nothing about the pack, the rides or the location
         is ever uploaded: there is no server, no account and no telemetry. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- This app is not on a store, so an update is a package it hands to the
         system installer. Android still shows its own confirmation prompt
         every time; this permission only allows asking. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />


    <uses-feature android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:label="JK BMS +"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Keeps a trip recording while you use another app or the screen is
             off, and carries the live notification. Location-typed because that
             is what grants continued GPS access. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="location|dataSync"
            android:exported="false" />
        <!-- The home screen widget. It only ever displays what was last read;
             it holds no connection and starts nothing. -->
        <receiver
            android:name=".PackWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/pack_widget_info" />
        </receiver>

        <!-- Grants the system installer read access to a downloaded update,

             and to nothing else. See res/xml/file_paths.xml. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Don't delete the meta-data below.

             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
